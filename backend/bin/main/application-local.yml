spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopdb?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true

jwt:
  secret: local-dev-secret-key-must-be-at-least-32-chars
  access-expiration: 1800000       # 30분
  refresh-expiration: 1209600000   # 14일

payment:
  toss:
    secret-key: test_sk_placeholder

cloud:
  aws:
    s3:
      bucket: local-bucket
    region: ap-northeast-2
    credentials:
      access-key: test
      secret-key: test
    endpoint: http://localhost:4566  # LocalStack
